{
  "name": "Ubuntu 22.04 Jammy",
  "image": "mcr.microsoft.com/vscode/devcontainers/base:ubuntu-22.04",

  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash"
  },

  "extensions": [],

  "forwardPorts": [3306, 5000],

  "postCreateCommand": "sudo apt-get update && sudo apt-get upgrade -y && sudo apt-get autoremove -y && sudo apt-get install -y mariadb-server && sudo service mariadb start && sudo mysql -e \"ALTER USER 'root'@'localhost' IDENTIFIED BY 'T3t0npack@160';\" && sudo apt-get install -y php php-mysql && sudo apt-get install -y wget && sudo wget https://www.adminer.org/latest.php -O /var/www/html/adminer.php && sudo service apache2 start"
}
